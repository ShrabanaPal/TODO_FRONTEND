import React from 'react'

const Logout = () => {
    
    const handleLogout = () => {
        return `{Login.jsx}`
    }
  return (
    <div>
      <div onClick={() => {handleLogout}}>Logout</div>
    </div>
  )
}

export default Logout
